二、请简述HashSet去除重复元素的原理。
通过hash值去和元素比较,
如果相同则
    比较equals方法是否相同.
        相同,为同一元素不存储
    .   不相同,存储
.如不相同则直接存储


//标注答案
	调用被添加元素的hashCode()，和HashSet中已有元素的hashCode比较是否相同
	如果不相同，直接存储
	如果相同，调用equals方法比较是否相同
	不相同，直接存储元素
	相同，认为是同一元素.不存储
